const AppHeader = () => {
  return (
    <header className="app-header mb-4">
      <h1 className="mb-2">React + GraphQL Dashboard</h1>
      <p className="mb-0">Apollo Server backend with a React Vite functional UI.</p>
    </header>
  );
};

export default AppHeader;